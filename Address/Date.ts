export class Date{
    constructor(private day: string, private month: string, private year: number, private time: string) {
        this.day = day;
        this.month = month;
        this.year = year;
        this.time = time;
    }
}